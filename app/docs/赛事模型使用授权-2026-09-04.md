# GlobalGuard 赛事专用模型使用授权记录

记录日期：2026-09-04。依据为用户本轮提供的赛事工作人员回复截图，署名王成。此处记录项目采用的授权依据，不声称已独立验证完整邮件头或发件人身份。

## 回复确认的范围

该回复明确表示赛事专用 API Key 可用于 GlobalGuard 参赛作品后端，包括商品图片与文案分析、开发测试，以及供评委访问的在线交互 Demo；并将其说明为赛事方向复赛队伍提供的专门授权。

因此，本项目此前“赛事后端使用权限待确认”的阻塞项已解除。此结论依据回复中的明确许可，不是以“API 能调用成功”代替授权判断，也不改变普通 Token Plan 的通用使用细则。

## 本地实施与边界

- 仅在当前本地环境开启 `MODEL_ROUTER_APP_USE_APPROVED=1`，`.env.example` 仍默认关闭，避免将本队授权套用于其他使用者。
- 原有密钥不变、不回显、不写入源码、测试报告、截图或 Git；无需再次发送密钥。
- 保留 HTTPS 固定端点、拒绝重定向、脱敏错误、超时和人工复核机制。
- 首轮仅用项目自有的合成演示图及其修复草稿进行小规模真实调用，不上传授权邮件或业务隐私资料给模型。
- 授权支持在线 Demo 用途，但不等于用户已要求现在公开部署、购买资源或发布仓库；这些动作仍另行确认。
- 回复未说明额度数值、授权到期时间、费用兜底或实际业务数据处理要求，不自行补充此类承诺。

## 来源留存

- 用户提供的原始文件仅在参赛者本机私密留存，公开仓库不记录其本地路径。
- 原图 SHA-256：`EC9447EAD217CFF84E830090E73687B2F960F5D4D0A3E4BD70661E83A32A1461`。
- 本地副本：`app/docs/evidence/token-plan-authorization-2026-09-04.png`。仅本地留存，尚未公开发布。

## 技术参考

本轮使用百炼文档技能核对图片数据 URL 请求格式，沿用固定赛事端点而非通用按量端点。技能参考相对路径：`wiki/concepts/openai-compatible-api.md`；在线依据：[图像与视频理解](https://help.aliyun.com/zh/model-studio/vision)。上述技术文档说明协议格式，不作为赛事特殊授权的证明。
## 仓库备份说明

2026-09-04：用户授权上传项目到其私有 GitHub 仓库。上述授权截图原件仅在本机保存，并由 `.gitignore` 排除；仓库保留文字摘要与原图哈希，不包含私人邮件原图。克隆仓库后缺少该截图是预期行为，不代表授权已失效。
